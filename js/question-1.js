// Convert the folowing function to an arrow function:

/*
function getRemainder(a,b) {
    return a % b;
}
*/

const getRemainder = (a,b) => a % b;

const result = getRemainder(9,5);

console.log(result);